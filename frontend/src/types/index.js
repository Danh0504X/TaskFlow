/**
 * Thư mục này thường dùng để định nghĩa các Types/Interfaces khi dùng TypeScript.
 * Vì dự án hiện tại dùng JavaScript, bạn có thể dùng JSDoc để khai báo cấu trúc dữ liệu,
 * điều này giúp các IDE gợi ý code (IntelliSense) chuẩn không kém gì TypeScript.
 */

/**
 * @typedef {Object} UserType
 * @property {string} _id
 * @property {string} fullName
 * @property {string} email
 * @property {string} avatarUrl
 * @property {string} status
 */

export const DEFAULT_USER = null;
